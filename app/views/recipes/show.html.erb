<h1>Recipe Name: <%= @recipe.name.capitalize %></h1>
<div class= "col-md-12">
    <h4 class="description"> Steps to create: </h4>
    <hr />
    <%= @recipe.description %>
    <br>
    <br>
    Created by:
    <%= @recipe.user.username %>
</div>

<div class= "col-md-12">
<div class="recipe-actions">
  <% if logged_in? && (current_user == @recipe.user || current_user.admin?) %>
    <%= link_to "Edit this recipe", edit_recipe_path(@recipe), 
                              class: "btn btn-xs btn-info" %>
    <%= link_to "Delete this recipe", recipe_path(@recipe), method: :delete,
                                        data: { confirm: "Are you sure you want to delete this recipe?" }, 
                                        class: "btn btn-xs btn-danger" %>
  <% end %>                                  
  <%= link_to "Return to recipes listing", recipes_path, class: "btn btn-xs btn-success" %>
</div>
</div>